# Core dependencies (required)
opencv-python>=4.5.0,<5.0.0    # Computer vision library for video processing
numpy>=1.20.0                  # Numerical operations, required by OpenCV
Pillow>=9.0.0                  # Python Imaging Library for image processing
smolagents>=0.1.0              # Agent architecture framework
requests>=2.25.0               # HTTP requests for Ollama API
python-dotenv>=0.19.0          # For loading environment variables

# Local inference dependencies
# No API keys needed when using Ollama

# OCR support (optional)
# pytesseract                  # Uncomment to enable OCR (requires Tesseract installation)

# Cloud API clients (optional, not needed for local execution)
# anthropic>=0.5.0             # Anthropic API client for Claude models
# openai>=1.0.0                # OpenAI API client for GPT models
# litellm>=0.1.0               # Unified interface for LLM APIs

# Utilities
tqdm>=4.62.0                   # Progress bars for batch processing
asyncio>=3.4.3                 # Asynchronous I/O

# Logging and error handling
loguru>=0.6.0                  # Enhanced logging capabilities
